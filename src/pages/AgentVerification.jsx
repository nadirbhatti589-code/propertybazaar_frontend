import { useEffect, useState } from 'react';
import api from '../api/axios';
const statusInfo = { pending: { color: 'bg-gold-400/20 text-gold-500', text: 'Your application is under review. This usually takes 1-2 business days.' }, verified: { color: 'bg-teal-50 text-teal-600', text: "You're a verified agent! A badge now appears on your listings." }, rejected: { color: 'bg-brand-50 text-brand-500', text: 'Your application was rejected. See the reason below.' } };
const Section = ({ title, children }) => <section className="border-t border-sand-200 pt-6"><h2 className="font-display text-xl font-semibold text-ink">{title}</h2><div className="mt-4 space-y-4">{children}</div></section>;
const AgentVerification = () => {
  const [agent, setAgent] = useState(null); const [loading, setLoading] = useState(true); const [form, setForm] = useState({ agencyName: '', licenseNumber: '', licenseDocumentUrl: '', bio: '', serviceCitiesText: '' }); const [error, setError] = useState(''); const [success, setSuccess] = useState(''); const [submitting, setSubmitting] = useState(false);
  useEffect(() => { const fetchProfile = async () => { try { const { data } = await api.get('/agents/me'); setAgent(data.agent); } catch (err) { /* no application submitted */ } finally { setLoading(false); } }; fetchProfile(); }, []);
  const handleChange = (e) => setForm({ ...form, [e.target.name]: e.target.value });
  const handleSubmit = async (e) => { e.preventDefault(); setError(''); setSuccess(''); setSubmitting(true); try { const payload = { agencyName: form.agencyName, licenseNumber: form.licenseNumber, licenseDocumentUrl: form.licenseDocumentUrl, bio: form.bio, serviceCities: form.serviceCitiesText.split(',').map((c) => c.trim()).filter(Boolean) }; const { data } = await api.post('/agents/apply', payload); setAgent(data.agent); setSuccess('Application submitted! Track your status below.'); } catch (err) { setError(err.response?.data?.message || 'Failed to submit application'); } finally { setSubmitting(false); } };
  if (loading) return <p className="py-20 text-center text-sand-600">Loading...</p>;
  return <main className="mx-auto max-w-xl px-4 py-8"><p className="text-xs font-semibold uppercase tracking-[0.18em] text-teal-500">Build trust</p><h1 className="section-heading mt-1">Agent Verification</h1><p className="mt-2 text-sm leading-6 text-sand-600">Get a verified badge on your listings so buyers know you're a legitimate, licensed agent.</p>{agent ? <div className="mt-7 border border-sand-200 bg-white p-6"><span className={`inline-block rounded-full px-3 py-1 text-xs font-semibold capitalize ${statusInfo[agent.verifiedStatus]?.color || 'bg-sand-100 text-sand-600'}`}>{agent.verifiedStatus}</span><p className="mt-4 text-sand-600">{statusInfo[agent.verifiedStatus]?.text}</p>{agent.verifiedStatus === 'rejected' && agent.rejectionReason && <p className="mt-4 text-sm text-brand-500">Reason: {agent.rejectionReason}</p>}<div className="mt-5 space-y-1 border-t border-sand-200 pt-4 text-sm text-sand-600"><p><span className="font-semibold text-ink">Agency:</span> {agent.agencyName || '-'}</p><p><span className="font-semibold text-ink">License Number:</span> {agent.licenseNumber}</p>{agent.serviceCities?.length > 0 && <p><span className="font-semibold text-ink">Service Cities:</span> {agent.serviceCities.join(', ')}</p>}</div></div> : <form onSubmit={handleSubmit} className="mt-7 space-y-7 border border-sand-200 bg-white p-5 sm:p-7">{error && <p className="rounded-md bg-brand-50 px-3 py-2 text-sm text-brand-700">{error}</p>}{success && <p className="rounded-md bg-teal-50 px-3 py-2 text-sm text-teal-600">{success}</p>}<Section title="Professional Details"><div><label className="mb-1.5 block text-sm font-medium text-ink">Agency Name (optional)</label><input name="agencyName" value={form.agencyName} onChange={handleChange} className="form-input" /></div><div><label className="mb-1.5 block text-sm font-medium text-ink">License Number</label><input name="licenseNumber" required value={form.licenseNumber} onChange={handleChange} className="form-input" /></div></Section><Section title="Verification Documents"><div><label className="mb-1.5 block text-sm font-medium text-ink">License Document URL</label><input name="licenseDocumentUrl" required value={form.licenseDocumentUrl} onChange={handleChange} placeholder="Paste a link to your uploaded license document" className="form-input" /><p className="mt-1 text-xs leading-5 text-sand-600">Image upload isn't wired into this form yet — for now, paste a link to your document (e.g. uploaded to Google Drive with public view access, or a Cloudinary URL).</p></div></Section><Section title="Profile & Coverage"><div><label className="mb-1.5 block text-sm font-medium text-ink">Bio (optional)</label><textarea name="bio" rows={3} value={form.bio} onChange={handleChange} className="form-input resize-y" /></div><div><label className="mb-1.5 block text-sm font-medium text-ink">Service Cities (comma separated)</label><input name="serviceCitiesText" placeholder="Karachi, Lahore, Islamabad" value={form.serviceCitiesText} onChange={handleChange} className="form-input" /></div></Section><button type="submit" disabled={submitting} className="btn-primary w-full">{submitting ? 'Submitting...' : 'Submit Application'}</button></form>}</main>;
};
export default AgentVerification;
